#include <iostream>
#include <vector>
#include <algorithm>
#include <unordered_map>
using namespace std;

class Solution
{
public:
    int findTargetSumWays(vector<int> &nums, int target)
    {
        
    }
};

int main()
{
    vector<int> &v = {1, 1, 1, 1};
    int target = 4;
    Solution s1;
    cout << s1.findTargetSumWays(v, target);
    return 0;
}